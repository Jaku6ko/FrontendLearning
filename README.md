This is a movie search engine made with JS and HTML/CSS (with Bootstrap and Jquery frameworks) that uses an external, open-source movie databse API.
It lets the user search a movie by its title and  After pressing the "search" button, the program sends information to the API about a movie the user wants to find,
retrieves data from the database, andopens a page displaying all movies with the given phrase in the title.